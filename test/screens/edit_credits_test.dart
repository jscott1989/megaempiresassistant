import '../utils/test_utils.dart';

void main() {
  initTests();

  // Screenshot test

  // Minimum is those provided by purchased advances

  // Minimum takes into account "optional credits"

  // Increment works

  // Decrement works

  // Continue returns to whatever screen we were on before
}
