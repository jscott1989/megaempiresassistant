import '../utils/test_utils.dart';

void main() {
  initTests();

  // Screenshot test

  // Can't continue if we haven't given the correct amount

  // Can't reduce below the existing amount of any credit

  // Can't exceed the allowed amount

  // Pressing continue calls back correctly?
}
