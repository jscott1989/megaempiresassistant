import '../utils/test_utils.dart';

void main() {
  initTests();

  // Screenshot test (also fix overflow issue...)

  // Shows saved settings

  // Continue goes back

  // Continue saves settings

  // Continue doesn't change additional credits or advances

  // Continue doesn't work with invalid setup
}
